// imports
//import initial from '../initial';

// reducer 
const reducer = (state, action) => {
    switch (action.type) {
        // cases

        // default
        default: return state;
    };
};

export default reducer;